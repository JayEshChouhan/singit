import styled from "styled-components"

const UpdateFormWrapper = styled.form`
  text-align: center;
  h1 { margin-bottom: 0; }
  img { max-width: 100px; margin-bottom: -20px; }
  p { display: block; margin: 5px auto; font-size: 14px; color: var(--medium-text); max-width: 400px; }
  input { margin-top: 20px; }
`;

export default UpdateFormWrapper;
